'use client'

import {Button} from "rsuite";
import {getPrice} from "@/fsd/entities/open-question";


export const GetPriceButton = () => {
  // const

  const calculatePrice = async () => {
    // const res = await getPrice()
  }
  return (
    <Button>Рассчитать стоимость</Button>
  )
}